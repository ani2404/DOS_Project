bool pread(int dev_id){
	switch(dev_id){
		case 7:
		gpio_read(7,data)
		default: assert(false);		
		}	
	}
bool pwrite(){	
	switch(){	
		case 0:
		gpio_write(0,data)
		case 1:
		gpio_write(1,data)
		case 3:
		gpio_write(3,data)
		default: assert(false);
		}
	}
